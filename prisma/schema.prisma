generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Notes {
  serial    Int         @id @default(autoincrement())
  title     String
  content   String
  tags      String?
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
  Users     notesUser[]
  usersId   Int?
}

enum Roles {
  Admin
  Viewer
  Editor
}

model Users {
  id       Int         @id @default(autoincrement())
  name     String
  username String?     @unique
  email    String      @unique
  password String
  notes    notesUser[]
}

model notesUser {
  user    Users @relation(fields: [userId], references: [id])
  userId  Int
  note    Notes @relation(fields: [notesId], references: [serial])
  notesId Int

  role Roles

  @@id([userId, notesId])
}
